{
  "name": "redxt",
  "version": "2.0.0",
  "description": "Optimized Node-RED for Cloudflare Workers",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute rednox-flows --remote --command 'SELECT 1'",
    "db:create": "wrangler d1 create rednox-flows"
  },
    "devDependencies": {
    "@biomejs/biome": "^2.2.5",
    "@cloudflare/vitest-pool-workers": "^0.9.12",
    "@cloudflare/workers-types": "^4.20251008.0",
    "@types/node": "^24.7.1",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3",
    "vitest": "3.2.4",
    "wrangler": "^4.42.2"
  },
  "dependencies": {
    "@google/genai": "^1.28.0",
    "itty-router": "^5.0.0",
    "@anthropic-ai/sdk": "^0.20.0"
  }
}
